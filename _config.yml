title: "Work From Cafe — Direktori Aplikasi"
description: "Direktori resmi aplikasi Android dari Pengembang Work From Cafe."
url: "https://dev.androidhebat.com"
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter_opts:
    disable: true

collections:
  policies:
    output: true
    permalink: /:path/

defaults:
  - scope:
      path: ""
      type: policies
    values:
      layout: policy
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
